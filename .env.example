# Optional: Gemini API key for AI image transformation (human → Spider-Man mask).
# Get a key at https://aistudio.google.com/apikey
# Without this, the site uses the Spider-Man asset from the assets folder for the scroll morph.
VITE_GEMINI_API_KEY=
